SET B - 

Develop a dashboard screen that shows a scrollable list of courses using LazyColumn, Each item should display the course title, instructor name and progress bar showing completion percentage. Also make each item
clickable so that tapping it shows a toast with course info. add a button to refresh progress values.
